# Environment
COB Internal Infrastructure Provisioning Platform capabilities can be reused across different environments while allowing appropriate configuration differences.

The enviroment variable in the root module and child module can be changed to the required environment to reflect changes across the configuration files, there by avoids duplicating infrastructure code.

### Available Envrionments
* Dev
* Production
* Staging
